using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PersistentData : MonoBehaviour
{
    /*public static PersistentData data;
    public bool player2Active = false;

    //gets called on entering the scene
    void Awake()
    {
        //prevents from detroying p2 if p2 is spawn
        if(data == null)
        {
            DontDestroyOnLoad(gameObject);
            data = this;
        }
        else if(data != this)
        {
            Destroy(gameObject);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
